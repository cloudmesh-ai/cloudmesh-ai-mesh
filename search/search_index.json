{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"Home","text":""},{"location":"index.html#cloudmesh-ai-mesh","title":"Cloudmesh AI Mesh","text":"<p>Cloudmesh AI Mesh is a management layer for a local AI development cluster. The system operates entirely offline, ensuring that all data and inference remain within the local network.</p> <p>Quick Links: - API Reference - Full technical documentation of all modules.</p>"},{"location":"index.html#cluster-architecture","title":"\ud83c\udfd7 Cluster Architecture","text":"<p>The cluster consists of three primary components:</p>"},{"location":"index.html#1-white-primary-compute-node","title":"1. WHITE (Primary Compute Node)","text":"<ul> <li>Hardware: RTX 3090 GPU, 128GB RAM.</li> <li>Services:<ul> <li>vLLM: High-performance GPU inference server.</li> <li>LiteLLM: OpenAI-compatible API gateway (port 4000) that routes requests to available backends.</li> </ul> </li> </ul>"},{"location":"index.html#2-spark-fallback-node","title":"2. SPARK (Fallback Node)","text":"<ul> <li>Hardware: CPU / Lightweight GPU.</li> <li>Services:<ul> <li>llama.cpp: Inference server (port 8080) for GGUF quantized models.</li> <li>Model Management: Supports optional on-demand loading and unloading of GGUF models to optimize memory usage.</li> </ul> </li> </ul>"},{"location":"index.html#3-laptop-control-plane","title":"3. LAPTOP (Control Plane)","text":"<ul> <li>Services:<ul> <li>VSCode: Development environment with AI extensions.</li> <li>SSH Orchestration: Tools for managing remote nodes.</li> <li>AI Agent: Python-based cluster management agent.</li> </ul> </li> </ul>"},{"location":"index.html#api-and-routing","title":"\ud83c\udf10 API and Routing","text":""},{"location":"index.html#core-entrypoint","title":"Core Entrypoint","text":"<p>All applications connect to the cluster via the LiteLLM gateway: <code>http://localhost:4000/v1/chat/completions</code></p>"},{"location":"index.html#routing-logic","title":"Routing Logic","text":"<p>LiteLLM manages request distribution based on task requirements and node availability: - High Performance: Requests are routed to WHITE (vLLM). - Fallback: If WHITE is unavailable, requests are routed to SPARK (llama.cpp). - Task-Specific Routing:     - Coding tasks $\\rightarrow$ Coding-specific models (e.g., DeepSeek Coder).     - Reasoning tasks $\\rightarrow$ Llama 3 class models.     - Autocomplete $\\rightarrow$ Small Mistral models.</p>"},{"location":"index.html#command-line-interface","title":"\ud83d\udcbb Command Line Interface","text":"<p>The cluster is managed via the <code>cmc mesh</code> command group.</p>"},{"location":"index.html#available-commands","title":"Available Commands","text":""},{"location":"index.html#cmc-mesh-status","title":"<code>cmc mesh status</code>","text":"<p>Displays the current GPU utilization and availability across all cluster nodes. It provides a table including node name, partition, state, and available vs. total GPUs.</p>"},{"location":"index.html#cmc-mesh-health","title":"<code>cmc mesh health</code>","text":"<p>Executes a diagnostic check of the environment, verifying: - VPN connectivity to the HPC environment. - SSH access to the compute nodes. - Disk quota availability.</p>"},{"location":"index.html#cmc-mesh-info","title":"<code>cmc mesh info</code>","text":"<p>Displays the static configuration and architecture of the AI Mesh cluster, including port configuration for all hosts and SSH status.</p>"},{"location":"index.html#cmc-mesh-claude","title":"<code>cmc mesh claude</code>","text":"<p>Starts the Claude AI environment using the local configuration and model.</p>"},{"location":"index.html#cmc-mesh-tunnel-action-host","title":"<code>cmc mesh tunnel &lt;action&gt; [&lt;host&gt;]</code>","text":"<p>Manage SSH tunnels for hosts in the mesh.</p> <p>Actions: - <code>start</code> - Start SSH tunnel(s) for host(s) - <code>stop</code> - Stop SSH tunnel(s) for host(s) - <code>status</code> - Check status of SSH tunnel(s)</p> <p>HOST: The hostname defined in config.yaml (optional for 'start' and 'stop')</p> <p>Examples: - <code>cmc mesh tunnel start white</code> - Start tunnel for specific host - <code>cmc mesh tunnel start</code> - Start tunnels for all SSH-enabled hosts - <code>cmc mesh tunnel stop spark</code> - Stop tunnel for specific host - <code>cmc mesh tunnel stop</code> - Stop all active tunnels - <code>cmc mesh tunnel status</code> - Check status of all tunnels</p>"},{"location":"index.html#cmc-mesh-set-host","title":"<code>cmc mesh set &lt;host&gt;</code>","text":"<p>Sets the default host for the AI Mesh cluster in the configuration.</p> <p>Example: - <code>cmc mesh set white</code> - Set 'white' as the default host</p>"},{"location":"index.html#installation-and-setup","title":"\ud83d\udee0 Installation and Setup","text":""},{"location":"index.html#compute-node-setup-white","title":"Compute Node Setup (WHITE)","text":""},{"location":"index.html#nvidia-drivers-and-cuda","title":"NVIDIA Drivers and CUDA","text":"<ol> <li>Update System:    <pre><code>docker run --rm -it ubuntu:latest /bin/bash -c \"apt-get update &amp;&amp; apt-get upgrade -y\"\n</code></pre></li> <li>Install Drivers:    <pre><code>docker run --rm -it nvidia/cuda:11.0-base /bin/bash -c \"nvidia-smi &amp;&amp; apt-get update &amp;&amp; apt-get install -y nvidia-driver-460\"\n</code></pre></li> <li>Install CUDA Toolkit:    <pre><code>docker run --rm -it nvidia/cuda:11.0-base /bin/bash -c \"apt-get update &amp;&amp; apt-get install -y nvidia-cuda-toolkit\"\n</code></pre></li> </ol>"},{"location":"index.html#inference-server-llamacpp","title":"Inference Server (llama.cpp)","text":"<p>To avoid manual compilation, the inference server is deployed using a Docker container.</p> <ol> <li>Model Deployment:    Use <code>huggingface-cli</code> to download GGUF models to the local <code>~/models</code> directory.</li> <li>Execution:    Run the server using the pre-built Docker image:    <pre><code>docker run -d --name llama-server \\\n  -v ~/models:/models \\\n  local/llama-cpp:latest \\\n  ./build/bin/llama-server \\\n    -m /models/qwen2.5-coder-32b-q4_k_m.gguf \\\n    --host 127.0.0.1 \\\n    --port 8080 \\\n    -ngl 999 \\\n    -c 32768\n</code></pre></li> </ol>"},{"location":"index.html#client-integration-laptop","title":"Client Integration (LAPTOP)","text":""},{"location":"index.html#vscode-configuration","title":"VSCode Configuration","text":"<p>Install the <code>Continue.dev</code> extension and configure the model provider: <pre><code>{\n  \"models\": [\n    {\n      \"title\": \"Qwen Local\",\n      \"provider\": \"openai\",\n      \"model\": \"qwen\",\n      \"apiBase\": \"http://127.0.0.1:8080/v1\",\n      \"apiKey\": \"local\"\n    }\n  ]\n}\n</code></pre></p>"},{"location":"index.html#operational-constraints","title":"\u26a0\ufe0f Operational Constraints","text":"<ul> <li>Local Only: No external API calls or internet-based inference are permitted.</li> <li>Explicit Execution: All system-level changes must be performed manually via explicit commands.</li> <li>Node Specification: Every command must specify the target machine (WHITE, SPARK, or LAPTOP) unless a default is set.</li> </ul>"},{"location":"CHANGELOG.html","title":"Changelog","text":""},{"location":"CHANGELOG.html#changelog","title":"Changelog","text":"<p>All notable changes to this project will be documented in this file.</p>"},{"location":"CHANGELOG.html#unreleased","title":"[Unreleased]","text":"<ul> <li>Initial project setup.</li> </ul>"},{"location":"PROMPT-README.html","title":"Local AI Development Cluster Documentation","text":""},{"location":"PROMPT-README.html#local-ai-development-cluster-documentation","title":"Local AI Development Cluster Documentation","text":""},{"location":"PROMPT-README.html#overview","title":"Overview","text":"<p>You are the controller for a fully local AI development cluster. The system is offline and uses no external APIs.</p>"},{"location":"PROMPT-README.html#cluster-architecture","title":"Cluster Architecture","text":""},{"location":"PROMPT-README.html#nodes","title":"Nodes","text":"<ol> <li>WHITE (primary compute node)</li> <li>Hardware: RTX 3090 + 128GB RAM</li> <li> <p>Services:</p> <ul> <li>vLLM (GPU inference server)</li> <li>LiteLLM (OpenAI-compatible API gateway on port 4000)</li> </ul> </li> <li> <p>SPARK (fallback node)</p> </li> <li>Hardware: CPU / lightweight GPU node</li> <li> <p>Services:</p> <ul> <li>llama.cpp server (port 8080)</li> <li>GGUF quantized models</li> </ul> </li> <li> <p>LAPTOP (client/control plane)</p> </li> <li>Services:<ul> <li>VSCode + extension</li> <li>SSH orchestration tools</li> <li>cluster management agent named <code>ai</code> (Python)</li> </ul> </li> </ol>"},{"location":"PROMPT-README.html#core-api-entrypoint","title":"Core API Entrypoint","text":"<p>All applications MUST use:</p> <pre><code>http://localhost:4000/v1/chat/completions\n</code></pre> <p>This endpoint is provided by LiteLLM running on WHITE.</p>"},{"location":"PROMPT-README.html#routing-behavior","title":"Routing Behavior","text":"<p>LiteLLM acts as the central router: - Routes requests to WHITE (vLLM) for high-performance inference. - Routes to SPARK (llama.cpp) as a fallback. - Handles model abstraction (OpenAI-compatible interface).</p> <p>Model switching is based on: - Task type - Performance requirements - Availability of nodes</p> <p>Example routing logic: - Coding tasks \u2192 coding model (e.g., DeepSeek Coder) - Reasoning tasks \u2192 Llama 3 class model - Fast autocomplete \u2192 small Mistral model - Fallback \u2192 SPARK llama.cpp</p>"},{"location":"PROMPT-README.html#spark-node-fallback-behavior","title":"SPARK Node (Fallback Behavior)","text":"<p>SPARK runs llama.cpp as a lightweight inference engine.</p> <p>It MAY optionally support a model swapping mechanism (\"llama-swap concept\"): - Allows switching between multiple GGUF models without restarting the server. - Loads models on demand. - Unloads inactive models to save memory. - Provides task-specific model selection on the SPARK node.</p> <p>IMPORTANT: - llama-swap is OPTIONAL and only applies to SPARK. - It does NOT replace LiteLLM routing. - It is only a local optimization for model management.</p>"},{"location":"PROMPT-README.html#white-node-primary-behavior","title":"WHITE Node (Primary Behavior)","text":"<p>WHITE runs: - vLLM for fast GPU inference. - LiteLLM for routing and API abstraction.</p> <p>WHITE is always the primary execution target unless unavailable.</p>"},{"location":"PROMPT-README.html#failure-handling","title":"Failure Handling","text":"<p>If WHITE fails: \u2192 automatically fallback to SPARK.</p> <p>If SPARK fails: \u2192 return structured error response (no external calls allowed).</p>"},{"location":"PROMPT-README.html#development-role","title":"Development Role","text":"<p>You act as: - AI coding assistant (Copilot replacement) - System orchestrator - Cluster-aware reasoning engine</p> <p>You may generate: - Code - Deployment commands - Debugging steps - System configuration updates</p> <p>BUT you must always: - Specify which machine each command runs on (WHITE / SPARK / LAPTOP). - Avoid assuming hidden system changes. - Keep execution steps explicit and manual unless instructed otherwise.</p>"},{"location":"PROMPT-README.html#safety-rules","title":"Safety Rules","text":"<ul> <li>The system is fully local.</li> <li>No internet APIs are allowed.</li> <li>No external model calls are permitted.</li> <li>All inference must stay within WHITE or SPARK.</li> <li>No silent execution of system-level commands.</li> <li>Always provide transparent steps for deployment actions.</li> </ul>"},{"location":"PROMPT-README.html#output-format-rule","title":"Output Format Rule","text":"<p>When giving instructions:</p> <p>Always structure responses as:</p> <p>\ud83d\udda5\ufe0f WHITE (RTX 3090 node) \u26a1 SPARK (fallback node) \ud83d\udcbb LAPTOP (control plane)</p> <p>and clearly separate commands per machine.</p>"},{"location":"PROMPT-README.html#installation-commands","title":"Installation Commands","text":""},{"location":"PROMPT-README.html#nvidia-drivers-cuda-white","title":"NVIDIA Drivers + CUDA (WHITE)","text":"<ol> <li> <p>Update System <pre><code>docker run --rm -it ubuntu:latest /bin/bash -c \"apt-get update &amp;&amp; apt-get upgrade -y\"\n</code></pre>     This command updates the package lists and upgrades all installed packages to their latest versions.</p> </li> <li> <p>Install NVIDIA Drivers <pre><code>docker run --rm -it nvidia/cuda:11.0-base /bin/bash -c \"nvidia-smi &amp;&amp; apt-get update &amp;&amp; apt-get install -y nvidia-driver-460\"\n</code></pre>     This command installs the recommended NVIDIA drivers automatically and then restarts the system to apply changes.</p> </li> <li> <p>Verify Installation <pre><code>docker run --rm -it nvidia/cuda:11.0-base /bin/bash -c \"nvidia-smi\"\n</code></pre>     This command verifies that the NVIDIA driver is correctly installed by displaying GPU information.</p> </li> </ol>"},{"location":"PROMPT-README.html#install-cuda-toolkit-white","title":"Install CUDA Toolkit (WHITE)","text":"<ol> <li> <p>Install CUDA Toolkit <pre><code>docker run --rm -it nvidia/cuda:11.0-base /bin/bash -c \"apt-get update &amp;&amp; apt-get install -y nvidia-cuda-toolkit\"\n</code></pre>     This command installs the CUDA toolkit, which is essential for running applications with GPU acceleration.</p> </li> <li> <p>Verify Installation <pre><code>docker run --rm -it nvidia/cuda:11.0-base /bin/bash -c \"nvcc --version\"\n</code></pre>     This command checks if the CUDA toolkit was installed correctly by displaying its version information.</p> </li> </ol>"},{"location":"PROMPT-README.html#install-llamacpp-white","title":"Install llama.cpp (WHITE)","text":"<ol> <li> <p>Clone Repository <pre><code>git clone https://github.com/ggml-org/llama.cpp\ncd llama.cpp\n</code></pre>     These commands clone the <code>llama.cpp</code> repository and navigate into it.</p> </li> <li> <p>Build With CUDA <pre><code>cmake -B build \\\n  -DGGML_CUDA=ON\ncmake --build build -j\n</code></pre>     These commands configure and build the project with CUDA support, enabling GPU acceleration.</p> </li> <li> <p>Verify Installation <pre><code>./build/bin/llama-cli --help\n</code></pre>     This command checks if <code>llama.cpp</code> was built correctly by displaying help information.</p> </li> </ol>"},{"location":"PROMPT-README.html#download-models-white","title":"Download Models (WHITE)","text":"<ol> <li> <p>Create Model Directory <pre><code>docker exec -it llama-server mkdir -p /models\ndocker exec -it llama-server cd /models\n</code></pre>     These commands create a directory for storing models and navigate into it.</p> </li> <li> <p>Install huggingface downloader <pre><code>docker run --rm -it local/llama-cpp:latest /bin/bash -c \"pip install -U huggingface_hub\"\n</code></pre>     This command installs the <code>huggingface_hub</code> package, which allows downloading models from Hugging Face.</p> </li> <li> <p>Download Qwen2.5-Coder <pre><code>docker run --rm -it local/llama-cpp:latest /bin/bash -c \"huggingface-cli download Qwen/Qwen2.5-Coder-32B-Instruct-GGUF --include '*Q4_K_M.gguf*' --local-dir /models\"\n</code></pre>     This command downloads the Qwen2.5-Coder model from Hugging Face.</p> </li> </ol>"},{"location":"PROMPT-README.html#run-inference-server-white","title":"Run Inference Server (WHITE)","text":"<ol> <li>Launch llama.cpp OpenAI-Compatible Server <pre><code>docker run -d \\\n  --name llama-server \\\n  -v ~/models:/models \\\n  local/llama-cpp:latest \\\n  ./build/bin/llama-server \\\n    -m /models/qwen2.5-coder-32b-q4_k_m.gguf \\\n    --host 127.0.0.1 \\\n    --port 8080 \\\n    -ngl 999 \\\n    -c 32768\n</code></pre>     These commands launch the <code>llama-server</code>, which serves as an OpenAI-compatible API endpoint for the Qwen2.5-Coder model.</li> </ol>"},{"location":"PROMPT-README.html#vscode-integration-laptop","title":"VSCode Integration (LAPTOP)","text":"<ol> <li> <p>Install Continue.dev     This step is not included in the current documentation, but you can install the Continue.dev extension in your VSCode environment.</p> </li> <li> <p>Configure Continue.dev <pre><code>{\n  \"models\": [\n    {\n      \"title\": \"Qwen Local\",\n      \"provider\": \"openai\",\n      \"model\": \"qwen\",\n      \"apiBase\": \"http://127.0.0.1:8080/v1\",\n      \"apiKey\": \"local\"\n    }\n  ]\n}\n</code></pre>     This JSON configuration allows VSCode to connect to the local AI server running on WHITE.</p> </li> </ol>"},{"location":"PROMPT-README.html#optional-open-webui-white","title":"Optional \u2014 Open WebUI (WHITE)","text":"<ol> <li> <p>Install Open WebUI <pre><code>docker run --rm -it local/llama-cpp:latest /bin/bash -c \"python -m venv venv &amp;&amp; source venv/bin/activate &amp;&amp; pip install open-webui\"\n</code></pre>     These commands set up a virtual environment and install the <code>open-webui</code> package.</p> </li> <li> <p>Launch Open WebUI <pre><code>docker exec -it llama-server /bin/bash -c \"source venv/bin/activate &amp;&amp; open-webui serve\"\n</code></pre>     This command starts the Open WebUI server, allowing you to interact with the AI model through a web interface.</p> </li> <li> <p>Connect to API URL     Access the Open WebUI at <code>http://127.0.0.1:8080</code> in your web browser.</p> </li> </ol>"},{"location":"api.html","title":"API Reference","text":""},{"location":"api.html#cloudmesh-ai-mesh-api-reference","title":"cloudmesh-ai-mesh API Reference","text":"<p>This page provides the API reference for the <code>cloudmesh-ai-mesh</code> library.</p>"},{"location":"api.html#api-documentation","title":"API Documentation","text":""},{"location":"api.html#cloudmesh.ai.mesh-modules","title":"Modules","text":""},{"location":"api.html#cloudmesh.ai.mesh.__init_","title":"__init_","text":""},{"location":"api.html#cloudmesh.ai.mesh.agent","title":"agent","text":""},{"location":"api.html#cloudmesh.ai.mesh.claude","title":"claude","text":""},{"location":"api.html#cloudmesh.ai.mesh.claude-modules","title":"Modules","text":""},{"location":"api.html#cloudmesh.ai.mesh.claude.manager","title":"manager","text":"<p>Claude Manager Implementation</p> <p>This module provides functionality to manage Claude AI settings and credentials.</p>"},{"location":"api.html#cloudmesh.ai.mesh.claude.manager-classes","title":"Classes","text":"ClaudeManager <p>Manages Claude AI settings and credentials.</p> Source code in <code>src/cloudmesh/ai/mesh/claude/manager.py</code> <pre><code>class ClaudeManager:\n    \"\"\"Manages Claude AI settings and credentials.\"\"\"\n\n    def __init__(self, hostname: str, config_dir: Optional[str] = None):\n        \"\"\"\n        Initializes the ClaudeManager.\n\n        Args:\n            hostname: The hostname of the target environment, used to locate configuration.\n            config_dir: Optional override for the configuration directory.\n        \"\"\"\n        self.hostname = hostname\n\n        if config_dir:\n            self.config_dir = config_dir\n        else:\n            # 1. Try the standard user-specific path: ~/.cloudmesh/ai/{hostname}/claude\n            user_path = Path(\"~/.cloudmesh/ai\").expanduser() / hostname / \"claude\"\n\n            # 2. Fallback to the discovered development path if the user path doesn't exist\n            dev_path = Path(\"/Users/grey/work/cloudmesh-ai-mesh/src/cloudmesh/ai/mesh/claude\")\n\n            if user_path.exists():\n                self.config_dir = str(user_path)\n            else:\n                self.config_dir = str(dev_path)\n\n        logger.debug(f\"ClaudeManager initialized for host {hostname} using config dir: {self.config_dir}\")\n\n    def get_settings(self) -&gt; Dict[str, Any]:\n        \"\"\"\n        Returns the Claude settings from settings.json.\n\n        Returns:\n            A dictionary containing the settings, or an empty dictionary if the file could not be loaded.\n        \"\"\"\n        path = os.path.join(self.config_dir, \"settings.json\")\n        try:\n            content = readfile(path)\n            return json.loads(content)\n        except Exception as e:\n            logger.error(f\"Failed to load Claude settings from {path}: {e}\")\n            return {}\n\n    def get_credentials(self) -&gt; Dict[str, Any]:\n        \"\"\"\n        Returns the Claude credentials from .credentials.json.\n\n        Returns:\n            A dictionary containing the credentials, or an empty dictionary if the file could not be loaded.\n        \"\"\"\n        path = os.path.join(self.config_dir, \".credentials.json\")\n        try:\n            content = readfile(path)\n            return json.loads(content)\n        except Exception as e:\n            logger.error(f\"Failed to load Claude credentials from {path}: {e}\")\n            return {}\n\n    def save_settings(self, settings: Dict[str, Any]) -&gt; str:\n        \"\"\"\n        Saves the Claude settings to ~/claude/settings.json if they have changed.\n\n        Args:\n            settings: The settings dictionary to save.\n\n        Returns:\n            A message indicating the result of the save operation.\n        \"\"\"\n        path = str(Path(\"~/claude/settings.json\").expanduser())\n        return self._save_json(path, settings, \"Settings\")\n\n    def save_credentials(self, credentials: Dict[str, Any]) -&gt; str:\n        \"\"\"\n        Saves the Claude credentials to ~/.claude/.credentials.json if they have changed.\n\n        Args:\n            credentials: The credentials dictionary to save.\n\n        Returns:\n            A message indicating the result of the save operation.\n        \"\"\"\n        path = str(Path(\"~/.claude/.credentials.json\").expanduser())\n        return self._save_json(path, credentials, \"Credentials\")\n\n    def _save_json(self, path: str, data: Dict[str, Any], label: str) -&gt; str:\n        \"\"\"\n        Internal helper to save JSON data with diff checking and user confirmation.\n\n        Args:\n            path: The destination file path.\n            data: The data to save.\n            label: A label for the data (e.g., \"Settings\") for logging and prompts.\n\n        Returns:\n            A message indicating whether the file was saved, unchanged, or if save was cancelled.\n        \"\"\"\n        try:\n            # Prepare new content as a sorted JSON string for consistent diffing\n            new_content = json.dumps(data, indent=2, sort_keys=True)\n\n            # Read existing content\n            existing_content = \"\"\n            if os.path.exists(path):\n                try:\n                    existing_content = readfile(path)\n                    # To avoid formatting differences, re-serialize the existing data\n                    existing_data = json.loads(existing_content)\n                    existing_content = json.dumps(existing_data, indent=2, sort_keys=True)\n                except Exception:\n                    # If existing file is not valid JSON, treat as empty for diffing\n                    existing_content = \"\"\n\n            # Check if content is the same\n            if existing_content == new_content:\n                return f\"The {label} file does not have to be changed.\"\n\n            # Show differences\n            print(f\"\\\\n--- Differences in {label} ({path}) ---\")\n            diff = difflib.unified_diff(\n                existing_content.splitlines(),\n                new_content.splitlines(),\n                fromfile=\"Existing\",\n                tofile=\"New\",\n                lineterm=\"\"\n            )\n            for line in diff:\n                print(line)\n            print(\"---------------------------------------\")\n\n            # Ask for confirmation\n            confirm = input(f\"Would you like to save these changes to {label}? (y/n): \").strip().lower()\n            if confirm == 'y':\n                writefile(path, new_content)\n                return f\"{label} saved successfully to {path}.\"\n            else:\n                return f\"Save for {label} cancelled by user.\"\n\n        except Exception as e:\n            logger.error(f\"Error during {label} save process to {path}: {e}\")\n            return f\"Failed to save {label}: {e}\"\n</code></pre> Methods: __init__ <pre><code>__init__(hostname, config_dir=None)\n</code></pre> <p>Initializes the ClaudeManager.</p> <p>Parameters:</p> Name Type Description Default <code>hostname</code> <code>str</code> <p>The hostname of the target environment, used to locate configuration.</p> required <code>config_dir</code> <code>Optional[str]</code> <p>Optional override for the configuration directory.</p> <code>None</code> Source code in <code>src/cloudmesh/ai/mesh/claude/manager.py</code> <pre><code>def __init__(self, hostname: str, config_dir: Optional[str] = None):\n    \"\"\"\n    Initializes the ClaudeManager.\n\n    Args:\n        hostname: The hostname of the target environment, used to locate configuration.\n        config_dir: Optional override for the configuration directory.\n    \"\"\"\n    self.hostname = hostname\n\n    if config_dir:\n        self.config_dir = config_dir\n    else:\n        # 1. Try the standard user-specific path: ~/.cloudmesh/ai/{hostname}/claude\n        user_path = Path(\"~/.cloudmesh/ai\").expanduser() / hostname / \"claude\"\n\n        # 2. Fallback to the discovered development path if the user path doesn't exist\n        dev_path = Path(\"/Users/grey/work/cloudmesh-ai-mesh/src/cloudmesh/ai/mesh/claude\")\n\n        if user_path.exists():\n            self.config_dir = str(user_path)\n        else:\n            self.config_dir = str(dev_path)\n\n    logger.debug(f\"ClaudeManager initialized for host {hostname} using config dir: {self.config_dir}\")\n</code></pre> get_credentials <pre><code>get_credentials()\n</code></pre> <p>Returns the Claude credentials from .credentials.json.</p> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>A dictionary containing the credentials, or an empty dictionary if the file could not be loaded.</p> Source code in <code>src/cloudmesh/ai/mesh/claude/manager.py</code> <pre><code>def get_credentials(self) -&gt; Dict[str, Any]:\n    \"\"\"\n    Returns the Claude credentials from .credentials.json.\n\n    Returns:\n        A dictionary containing the credentials, or an empty dictionary if the file could not be loaded.\n    \"\"\"\n    path = os.path.join(self.config_dir, \".credentials.json\")\n    try:\n        content = readfile(path)\n        return json.loads(content)\n    except Exception as e:\n        logger.error(f\"Failed to load Claude credentials from {path}: {e}\")\n        return {}\n</code></pre> get_settings <pre><code>get_settings()\n</code></pre> <p>Returns the Claude settings from settings.json.</p> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>A dictionary containing the settings, or an empty dictionary if the file could not be loaded.</p> Source code in <code>src/cloudmesh/ai/mesh/claude/manager.py</code> <pre><code>def get_settings(self) -&gt; Dict[str, Any]:\n    \"\"\"\n    Returns the Claude settings from settings.json.\n\n    Returns:\n        A dictionary containing the settings, or an empty dictionary if the file could not be loaded.\n    \"\"\"\n    path = os.path.join(self.config_dir, \"settings.json\")\n    try:\n        content = readfile(path)\n        return json.loads(content)\n    except Exception as e:\n        logger.error(f\"Failed to load Claude settings from {path}: {e}\")\n        return {}\n</code></pre> save_credentials <pre><code>save_credentials(credentials)\n</code></pre> <p>Saves the Claude credentials to ~/.claude/.credentials.json if they have changed.</p> <p>Parameters:</p> Name Type Description Default <code>credentials</code> <code>Dict[str, Any]</code> <p>The credentials dictionary to save.</p> required <p>Returns:</p> Type Description <code>str</code> <p>A message indicating the result of the save operation.</p> Source code in <code>src/cloudmesh/ai/mesh/claude/manager.py</code> <pre><code>def save_credentials(self, credentials: Dict[str, Any]) -&gt; str:\n    \"\"\"\n    Saves the Claude credentials to ~/.claude/.credentials.json if they have changed.\n\n    Args:\n        credentials: The credentials dictionary to save.\n\n    Returns:\n        A message indicating the result of the save operation.\n    \"\"\"\n    path = str(Path(\"~/.claude/.credentials.json\").expanduser())\n    return self._save_json(path, credentials, \"Credentials\")\n</code></pre> save_settings <pre><code>save_settings(settings)\n</code></pre> <p>Saves the Claude settings to ~/claude/settings.json if they have changed.</p> <p>Parameters:</p> Name Type Description Default <code>settings</code> <code>Dict[str, Any]</code> <p>The settings dictionary to save.</p> required <p>Returns:</p> Type Description <code>str</code> <p>A message indicating the result of the save operation.</p> Source code in <code>src/cloudmesh/ai/mesh/claude/manager.py</code> <pre><code>def save_settings(self, settings: Dict[str, Any]) -&gt; str:\n    \"\"\"\n    Saves the Claude settings to ~/claude/settings.json if they have changed.\n\n    Args:\n        settings: The settings dictionary to save.\n\n    Returns:\n        A message indicating the result of the save operation.\n    \"\"\"\n    path = str(Path(\"~/claude/settings.json\").expanduser())\n    return self._save_json(path, settings, \"Settings\")\n</code></pre>"},{"location":"api.html#cloudmesh.ai.mesh.claude.manager-functions","title":"Functions:","text":""},{"location":"api.html#cloudmesh.ai.mesh.config","title":"config","text":""},{"location":"api.html#cloudmesh.ai.mesh.config-classes","title":"Classes","text":""},{"location":"api.html#cloudmesh.ai.mesh.config.MeshConfig","title":"MeshConfig","text":"<p>               Bases: <code>Config</code></p> <p>Configuration for the AI Mesh cluster.</p> Source code in <code>src/cloudmesh/ai/mesh/config.py</code> <pre><code>class MeshConfig(Config):\n    \"\"\"Configuration for the AI Mesh cluster.\"\"\"\n\n    DEFAULT_CONFIG_PATH = Path(\"~/.config/cloudmesh/ai-mesh.yaml\").expanduser()\n\n    DEFAULTS = {}\n\n    SCHEMA = {\n        \"cloudmesh.ai.mesh.servers.inference.server\": {\"type\": str},\n        \"cloudmesh.ai.mesh.servers.inference.port\": {\"type\": int},\n        \"cloudmesh.ai.mesh.logging.level\": {\"type\": str},\n        \"cloudmesh.ai.mesh.logging.json_format\": {\"type\": bool},\n        \"cloudmesh.ai.mesh.logging.log_dir\": {\"type\": str},\n    }\n\n    def _load_config(self):\n        \"\"\"Loads configuration from bundled config.yaml and then user overrides.\"\"\"\n        # 1. Load bundled config.yaml as base defaults using importlib.resources\n        try:\n            # Access the config.yaml file within the cloudmesh.ai.mesh package\n            with importlib.resources.files(\"cloudmesh.ai.mesh\").joinpath(\"config.yaml\").open(\"r\") as f:\n                bundled_data = yaml.safe_load(f)\n                if bundled_data:\n                    self.data.update(bundled_data)\n        except Exception as e:\n            logger.warning(f\"Could not load bundled config config.yaml: {e}\")\n\n        # 2. Load user overrides from the config path\n        if self.path.exists():\n            try:\n                with open(self.path, \"r\") as f:\n                    user_config = yaml.safe_load(f)\n                    if user_config:\n                        self.data.update(user_config)\n            except Exception as e:\n                logger.warning(f\"Could not load user config file {self.path}: {e}\")\n\n        # 3. Apply logging configuration to ai-common logging system\n        self._apply_logging_config()\n\n    def _apply_logging_config(self):\n        \"\"\"Pushes logging settings from the config to the ai-common logging utility.\"\"\"\n        try:\n            logging_cfg = self.get(\"cloudmesh.ai.mesh.logging\")\n            if logging_cfg and isinstance(logging_cfg, dict):\n                # Update the global config in ai-common.logging\n                ai_log._logging_config.update(logging_cfg)\n        except Exception as e:\n            logger.warning(f\"Failed to apply logging config: {e}\")\n</code></pre>"},{"location":"api.html#cloudmesh.ai.mesh.config-modules","title":"Modules","text":""},{"location":"api.html#cloudmesh.ai.mesh.config_manager","title":"config_manager","text":""},{"location":"api.html#cloudmesh.ai.mesh.config_manager-classes","title":"Classes","text":""},{"location":"api.html#cloudmesh.ai.mesh.config_manager.MeshConfigManager","title":"MeshConfigManager","text":"<p>Manages configuration for the AI Mesh.</p> Source code in <code>src/cloudmesh/ai/mesh/config_manager.py</code> <pre><code>class MeshConfigManager:\n    \"\"\"\n    Manages configuration for the AI Mesh.\n    \"\"\"\n\n    def __init__(self):\n        self.config = config\n\n    def get_servers_config(self):\n        \"\"\"Returns the dictionary of all configured servers.\"\"\"\n        return self.config.get(\"cloudmesh.ai.mesh.servers\")\n\n    def get_inference_server(self):\n        \"\"\"Retrieves the global inference server setting.\"\"\"\n        return self.config.get(\"cloudmesh.ai.mesh.servers.inference.server\")\n\n    def get_config(self, key: str):\n        \"\"\"Gets a config value using a short key mapping.\"\"\"\n        full_key = self._map_key(key)\n        return self.config.get(full_key)\n\n    def set_config(self, key: str, value):\n        \"\"\"Sets a config value using a short key mapping.\"\"\"\n        full_key = self._map_key(key)\n        self.config.set(full_key, value)\n        self.config.save()\n\n    def _map_key(self, key: str) -&gt; str:\n        \"\"\"Maps short CLI keys to full config paths.\"\"\"\n        mapping = {\n            \"inference.server\": \"cloudmesh.ai.mesh.servers.inference.server\",\n            \"inference.port\": \"cloudmesh.ai.mesh.servers.inference.port\",\n            \"default\": \"cloudmesh.ai.mesh.default\",\n        }\n        return mapping.get(key, key)\n</code></pre>"},{"location":"api.html#cloudmesh.ai.mesh.config_manager.MeshConfigManager-functions","title":"Methods:","text":"get_config <pre><code>get_config(key)\n</code></pre> <p>Gets a config value using a short key mapping.</p> Source code in <code>src/cloudmesh/ai/mesh/config_manager.py</code> <pre><code>def get_config(self, key: str):\n    \"\"\"Gets a config value using a short key mapping.\"\"\"\n    full_key = self._map_key(key)\n    return self.config.get(full_key)\n</code></pre> get_inference_server <pre><code>get_inference_server()\n</code></pre> <p>Retrieves the global inference server setting.</p> Source code in <code>src/cloudmesh/ai/mesh/config_manager.py</code> <pre><code>def get_inference_server(self):\n    \"\"\"Retrieves the global inference server setting.\"\"\"\n    return self.config.get(\"cloudmesh.ai.mesh.servers.inference.server\")\n</code></pre> get_servers_config <pre><code>get_servers_config()\n</code></pre> <p>Returns the dictionary of all configured servers.</p> Source code in <code>src/cloudmesh/ai/mesh/config_manager.py</code> <pre><code>def get_servers_config(self):\n    \"\"\"Returns the dictionary of all configured servers.\"\"\"\n    return self.config.get(\"cloudmesh.ai.mesh.servers\")\n</code></pre> set_config <pre><code>set_config(key, value)\n</code></pre> <p>Sets a config value using a short key mapping.</p> Source code in <code>src/cloudmesh/ai/mesh/config_manager.py</code> <pre><code>def set_config(self, key: str, value):\n    \"\"\"Sets a config value using a short key mapping.\"\"\"\n    full_key = self._map_key(key)\n    self.config.set(full_key, value)\n    self.config.save()\n</code></pre>"},{"location":"api.html#cloudmesh.ai.mesh.manager","title":"manager","text":""},{"location":"api.html#cloudmesh.ai.mesh.manager-classes","title":"Classes","text":""},{"location":"api.html#cloudmesh.ai.mesh.manager.MeshManager","title":"MeshManager","text":"<p>Manages high-level operations and observation of the AI Mesh.</p> Source code in <code>src/cloudmesh/ai/mesh/manager.py</code> <pre><code>class MeshManager:\n    \"\"\"\n    Manages high-level operations and observation of the AI Mesh.\n    \"\"\"\n\n    def __init__(self, config_manager: MeshConfigManager = None):\n        self.config_manager = config_manager or MeshConfigManager()\n\n    def get_cluster_info(self) -&gt; dict:\n        \"\"\"\n        Retrieves general information about the AI Mesh cluster.\n        \"\"\"\n        server = self.config_manager.get_inference_server()\n        return {\n            \"inference_server\": server,\n            \"primary_node\": \"WHITE (RTX 3090)\",\n            \"fallback_node\": \"SPARK (CPU/Lightweight GPU)\",\n            \"control_plane\": \"LAPTOP\",\n            \"api_gateway\": \"LiteLLM (port 4000)\"\n        }\n\n    def get_gpu_status(self) -&gt; list:\n        \"\"\"\n        Fetches real-time GPU usage from the cluster.\n        \"\"\"\n        try:\n            from cloudmesh.ai.hpc.hpc import Hpc\n            hpc = Hpc()\n            return hpc.get_cluster_gpu_usage()\n        except ImportError:\n            logger.error(\"ai-hpc package is required for GPU status check.\")\n            raise ImportError(\"ai-hpc package is required for GPU status check.\")\n        except Exception as e:\n            logger.error(f\"Failed to fetch GPU status: {e}\")\n            raise\n\n    def run_health_check(self):\n        \"\"\"\n        Performs a health check of the HPC environment.\n        \"\"\"\n        try:\n            from cloudmesh.ai.hpc.hpc import Hpc\n            hpc = Hpc()\n            hpc.check()\n            return True\n        except ImportError:\n            logger.error(\"ai-hpc package is required for health check.\")\n            raise ImportError(\"ai-hpc package is required for health check.\")\n        except Exception as e:\n            logger.error(f\"Health check failed: {e}\")\n            raise\n</code></pre>"},{"location":"api.html#cloudmesh.ai.mesh.manager.MeshManager-functions","title":"Methods:","text":"get_cluster_info <pre><code>get_cluster_info()\n</code></pre> <p>Retrieves general information about the AI Mesh cluster.</p> Source code in <code>src/cloudmesh/ai/mesh/manager.py</code> <pre><code>def get_cluster_info(self) -&gt; dict:\n    \"\"\"\n    Retrieves general information about the AI Mesh cluster.\n    \"\"\"\n    server = self.config_manager.get_inference_server()\n    return {\n        \"inference_server\": server,\n        \"primary_node\": \"WHITE (RTX 3090)\",\n        \"fallback_node\": \"SPARK (CPU/Lightweight GPU)\",\n        \"control_plane\": \"LAPTOP\",\n        \"api_gateway\": \"LiteLLM (port 4000)\"\n    }\n</code></pre> get_gpu_status <pre><code>get_gpu_status()\n</code></pre> <p>Fetches real-time GPU usage from the cluster.</p> Source code in <code>src/cloudmesh/ai/mesh/manager.py</code> <pre><code>def get_gpu_status(self) -&gt; list:\n    \"\"\"\n    Fetches real-time GPU usage from the cluster.\n    \"\"\"\n    try:\n        from cloudmesh.ai.hpc.hpc import Hpc\n        hpc = Hpc()\n        return hpc.get_cluster_gpu_usage()\n    except ImportError:\n        logger.error(\"ai-hpc package is required for GPU status check.\")\n        raise ImportError(\"ai-hpc package is required for GPU status check.\")\n    except Exception as e:\n        logger.error(f\"Failed to fetch GPU status: {e}\")\n        raise\n</code></pre> run_health_check <pre><code>run_health_check()\n</code></pre> <p>Performs a health check of the HPC environment.</p> Source code in <code>src/cloudmesh/ai/mesh/manager.py</code> <pre><code>def run_health_check(self):\n    \"\"\"\n    Performs a health check of the HPC environment.\n    \"\"\"\n    try:\n        from cloudmesh.ai.hpc.hpc import Hpc\n        hpc = Hpc()\n        hpc.check()\n        return True\n    except ImportError:\n        logger.error(\"ai-hpc package is required for health check.\")\n        raise ImportError(\"ai-hpc package is required for health check.\")\n    except Exception as e:\n        logger.error(f\"Health check failed: {e}\")\n        raise\n</code></pre>"},{"location":"api.html#cloudmesh.ai.mesh.manager-functions","title":"Functions:","text":""},{"location":"api.html#cloudmesh.ai.mesh.probe","title":"probe","text":""},{"location":"api.html#cloudmesh.ai.mesh.prober","title":"prober","text":""},{"location":"api.html#cloudmesh.ai.mesh.prober-classes","title":"Classes","text":""},{"location":"api.html#cloudmesh.ai.mesh.prober.MeshProber","title":"MeshProber","text":"<p>Probes the versions of inference servers across the mesh.</p> Source code in <code>src/cloudmesh/ai/mesh/prober.py</code> <pre><code>class MeshProber:\n    \"\"\"\n    Probes the versions of inference servers across the mesh.\n    \"\"\"\n\n    def __init__(self, config_manager: MeshConfigManager = None):\n        self.config_manager = config_manager or MeshConfigManager()\n\n    def get_nodes_to_probe(self) -&gt; list:\n        \"\"\"\n        Parse configuration to identify nodes to probe.\n        \"\"\"\n        servers_config = self.config_manager.get_servers_config()\n        if not servers_config:\n            return []\n\n        nodes_to_probe = []\n        for host, details in servers_config.items():\n            server_type = details.get(\"server\")\n            # Normalize olama -&gt; ollama\n            if server_type == \"olama\":\n                server_type = \"ollama\"\n\n            model = details.get(\"model\")\n            ssh = details.get(\"ssh\", False)\n            port_cfg = details.get(\"port\", {})\n\n            if isinstance(port_cfg, dict):\n                remote_port = port_cfg.get(\"remote\")\n                local_port = port_cfg.get(\"local\", remote_port)\n            else:\n                remote_port = port_cfg\n                local_port = port_cfg\n\n            nodes_to_probe.append({\n                \"host\": host,\n                \"server\": server_type,\n                \"port\": remote_port,\n                \"local_port\": local_port,\n                \"model\": model,\n                \"auth\": \"SSH\" if ssh else \"-\",\n                \"ssh\": ssh,\n                \"auth_key\": details.get(\"auth_key\")\n            })\n        return nodes_to_probe\n\n    def _probe_node(self, node) -&gt; dict:\n        \"\"\"\n        Probes a single node and returns the result.\n        \"\"\"\n        host = node[\"host\"]\n        server_type = node[\"server\"]\n        remote_port = node[\"port\"]\n        local_port = node[\"local_port\"]\n        target_model = node[\"model\"]\n        auth_type = node[\"auth\"]\n        ssh = node[\"ssh\"]\n        auth_key = node.get(\"auth_key\")\n\n        probe_port = local_port if not ssh else remote_port\n\n        try:\n            if server_type == \"ollama\":\n                server = OllamaServer(host, probe_port, \"ollama\", auth_key=auth_key, ssh=ssh)\n            elif server_type == \"vllm\":\n                server = VllmServer(host, probe_port, \"vllm\", auth_key=auth_key, ssh=ssh)\n            else:\n                logger.warning(f\"Unsupported server type {server_type} for host {host}\")\n                return None\n\n            probe_result = server.probe()\n            model_status = server.check_model(target_model, probe_result[\"models\"])\n\n            return {\n                \"host\": host,\n                \"server\": server_type,\n                \"version\": probe_result[\"version\"],\n                \"config_model\": target_model,\n                \"model_status\": model_status,\n                \"auth\": auth_type,\n                \"ports\": f\"R:{remote_port}/L:{local_port}\"\n            }\n        except Exception as e:\n            logger.error(f\"Failed to probe {host}: {e}\")\n            return {\n                \"host\": host,\n                \"server\": server_type,\n                \"version\": \"ERROR\",\n                \"config_model\": target_model,\n                \"model_status\": \"\u2717\",\n                \"auth\": auth_type,\n                \"ports\": f\"R:{remote_port}/L:{local_port}\",\n                \"error\": str(e)\n            }\n\n    def probe_all(self) -&gt; list:\n        \"\"\"\n        Probe all configured servers and return structured results.\n        \"\"\"\n        nodes = self.get_nodes_to_probe()\n        if not nodes:\n            return []\n\n        with concurrent.futures.ThreadPoolExecutor() as executor:\n            # Use map to execute _probe_node in parallel\n            results = list(executor.map(self._probe_node, nodes))\n\n        # Filter out None results (from unsupported server types)\n        return [r for r in results if r is not None]\n</code></pre>"},{"location":"api.html#cloudmesh.ai.mesh.prober.MeshProber-functions","title":"Methods:","text":"get_nodes_to_probe <pre><code>get_nodes_to_probe()\n</code></pre> <p>Parse configuration to identify nodes to probe.</p> Source code in <code>src/cloudmesh/ai/mesh/prober.py</code> <pre><code>def get_nodes_to_probe(self) -&gt; list:\n    \"\"\"\n    Parse configuration to identify nodes to probe.\n    \"\"\"\n    servers_config = self.config_manager.get_servers_config()\n    if not servers_config:\n        return []\n\n    nodes_to_probe = []\n    for host, details in servers_config.items():\n        server_type = details.get(\"server\")\n        # Normalize olama -&gt; ollama\n        if server_type == \"olama\":\n            server_type = \"ollama\"\n\n        model = details.get(\"model\")\n        ssh = details.get(\"ssh\", False)\n        port_cfg = details.get(\"port\", {})\n\n        if isinstance(port_cfg, dict):\n            remote_port = port_cfg.get(\"remote\")\n            local_port = port_cfg.get(\"local\", remote_port)\n        else:\n            remote_port = port_cfg\n            local_port = port_cfg\n\n        nodes_to_probe.append({\n            \"host\": host,\n            \"server\": server_type,\n            \"port\": remote_port,\n            \"local_port\": local_port,\n            \"model\": model,\n            \"auth\": \"SSH\" if ssh else \"-\",\n            \"ssh\": ssh,\n            \"auth_key\": details.get(\"auth_key\")\n        })\n    return nodes_to_probe\n</code></pre> probe_all <pre><code>probe_all()\n</code></pre> <p>Probe all configured servers and return structured results.</p> Source code in <code>src/cloudmesh/ai/mesh/prober.py</code> <pre><code>def probe_all(self) -&gt; list:\n    \"\"\"\n    Probe all configured servers and return structured results.\n    \"\"\"\n    nodes = self.get_nodes_to_probe()\n    if not nodes:\n        return []\n\n    with concurrent.futures.ThreadPoolExecutor() as executor:\n        # Use map to execute _probe_node in parallel\n        results = list(executor.map(self._probe_node, nodes))\n\n    # Filter out None results (from unsupported server types)\n    return [r for r in results if r is not None]\n</code></pre>"},{"location":"api.html#cloudmesh.ai.mesh.prober-functions","title":"Functions:","text":""},{"location":"api.html#cloudmesh.ai.mesh.servers","title":"servers","text":""},{"location":"api.html#cloudmesh.ai.mesh.servers-classes","title":"Classes","text":""},{"location":"api.html#cloudmesh.ai.mesh.servers.OllamaServer","title":"OllamaServer","text":"<p>               Bases: <code>BaseServer</code></p> <p>Ollama server probe implementation.</p> Source code in <code>src/cloudmesh/ai/mesh/servers/ollama.py</code> <pre><code>class OllamaServer(BaseServer):\n    \"\"\"Ollama server probe implementation.\"\"\"\n\n    def probe(self) -&gt; dict:\n        try:\n            # Get models\n            models_data = self._request(\"/api/tags\").get(\"models\", [])\n            models = [m.get(\"name\") for m in models_data]\n\n            # Get version - Ollama's /api/version returns a JSON object\n            version = \"Unknown\"\n            try:\n                v_data = self._request(\"/api/version\", timeout=1)\n                version = v_data.get(\"version\", \"Unknown\")\n            except:\n                pass\n\n            return {\n                \"status\": \"up\",\n                \"version\": version,\n                \"models\": models,\n                \"error\": None\n            }\n        except Exception as e:\n            # Use console.error instead of logger to avoid the context_id logging error\n            console.error(f\"Failed to get models from Ollama on {self.host}: {e}\")\n            return {\n                \"status\": \"down\",\n                \"version\": \"N/A\",\n                \"models\": [],\n                \"error\": str(e)\n            }\n</code></pre>"},{"location":"api.html#cloudmesh.ai.mesh.servers.VllmServer","title":"VllmServer","text":"<p>               Bases: <code>BaseServer</code></p> <p>vLLM server probe implementation.</p> Source code in <code>src/cloudmesh/ai/mesh/servers/vllm.py</code> <pre><code>class VllmServer(BaseServer):\n    \"\"\"vLLM server probe implementation.\"\"\"\n\n    def get_models(self) -&gt; list:\n        \"\"\"Queries the vLLM server for available models.\"\"\"\n        try:\n            # vLLM typically follows OpenAI API\n            models_data = self._request(\"/v1/models\").get(\"data\", [])\n            return [m.get(\"id\") for m in models_data]\n        except Exception as e:\n            logger.debug(f\"Failed to query vLLM models on {self.host}: {e}\")\n            return []\n\n    def probe(self) -&gt; dict:\n        try:\n            models = self.get_models()\n\n            # vLLM doesn't have a standard /version endpoint like Ollama\n            # Often the model name is the primary identifier\n            version = models[0] if models else \"Unknown\"\n\n            return {\n                \"status\": \"up\",\n                \"version\": version,\n                \"models\": models,\n                \"error\": None\n            }\n        except Exception as e:\n            # Use console.error instead of logger to avoid the context_id logging error\n            console.error(f\"Failed to get models from vLLM on {self.host}: {e}\")\n            return {\n                \"status\": \"down\",\n                \"version\": \"N/A\",\n                \"models\": [],\n                \"error\": str(e)\n            }\n</code></pre>"},{"location":"api.html#cloudmesh.ai.mesh.servers.VllmServer-functions","title":"Methods:","text":"get_models <pre><code>get_models()\n</code></pre> <p>Queries the vLLM server for available models.</p> Source code in <code>src/cloudmesh/ai/mesh/servers/vllm.py</code> <pre><code>def get_models(self) -&gt; list:\n    \"\"\"Queries the vLLM server for available models.\"\"\"\n    try:\n        # vLLM typically follows OpenAI API\n        models_data = self._request(\"/v1/models\").get(\"data\", [])\n        return [m.get(\"id\") for m in models_data]\n    except Exception as e:\n        logger.debug(f\"Failed to query vLLM models on {self.host}: {e}\")\n        return []\n</code></pre>"},{"location":"api.html#cloudmesh.ai.mesh.servers-modules","title":"Modules","text":""},{"location":"api.html#cloudmesh.ai.mesh.servers.base","title":"base","text":""},{"location":"api.html#cloudmesh.ai.mesh.servers.base-classes","title":"Classes","text":"BaseServer <p>               Bases: <code>ABC</code></p> <p>Base class for mesh inference servers.</p> Source code in <code>src/cloudmesh/ai/mesh/servers/base.py</code> <pre><code>class BaseServer(ABC):\n    \"\"\"Base class for mesh inference servers.\"\"\"\n\n    def __init__(self, host: str, port: int, server_type: str, auth_key: str = None, ssh: bool = False):\n        self.host = host\n        self.port = port\n        self.server_type = server_type\n\n        # If auth_key is a path to a file, read its content\n        if auth_key:\n            expanded_path = os.path.expanduser(auth_key)\n            if os.path.exists(expanded_path) and os.path.isfile(expanded_path):\n                try:\n                    with open(expanded_path, 'r') as f:\n                        auth_key = f.read().strip()\n                except Exception as e:\n                    logger.error(f\"Failed to read auth_key file {expanded_path}: {e}\")\n\n        self.auth_key = auth_key\n        self.ssh = ssh\n        self.url = f\"http://{host}:{port}\"\n\n    def _get_headers(self):\n        headers = {}\n        if self.auth_key:\n            headers[\"Authorization\"] = f\"Bearer {self.auth_key}\"\n        return headers\n\n    def _request(self, endpoint: str, timeout: int = 2):\n        \"\"\"Perform an HTTP request, either locally or via SSH.\"\"\"\n        if not self.ssh:\n            resp = requests.get(f\"{self.url}{endpoint}\", headers=self._get_headers(), timeout=timeout)\n            resp.raise_for_status()\n            return resp.json()\n        else:\n            # Use curl on the remote host targeting localhost\n            url = f\"http://localhost:{self.port}{endpoint}\"\n\n            header_args = []\n            if self.auth_key:\n                header_args.append(f\"-H 'Authorization: Bearer {self.auth_key}'\")\n\n            header_str = \" \".join(header_args)\n            curl_cmd = f\"curl -s {header_str} {url}\".strip()\n\n            try:\n                result = subprocess.run(\n                    [\"ssh\", self.host, curl_cmd],\n                    capture_output=True,\n                    text=True,\n                    timeout=timeout\n                )\n                if result.returncode != 0:\n                    raise Exception(f\"SSH command failed with exit code {result.returncode}: {result.stderr}\")\n\n                return json.loads(result.stdout)\n            except subprocess.TimeoutExpired:\n                raise Exception(f\"SSH request to {self.host} timed out after {timeout}s\")\n            except json.JSONDecodeError:\n                raise Exception(f\"Failed to parse JSON response from {self.host}: {result.stdout}\")\n\n    @abstractmethod\n    def probe(self) -&gt; dict:\n        \"\"\"\n        Probes the server for version and model availability.\n        Returns a dictionary with 'version', 'models', and 'status'.\n        \"\"\"\n        pass\n\n    def check_model(self, model_name: str, available_models: list) -&gt; str:\n        \"\"\"Returns a checkmark or cross based on model availability.\"\"\"\n        if not available_models:\n            return \"\u2717\"\n\n        # Check if the configured model is among the available models\n        if any(model_name == m for m in available_models):\n            return \"\u2713\"\n\n        # If different, show the first available model\n        return f\"\u2717 {available_models[0]}\"\n</code></pre> Methods: check_model <pre><code>check_model(model_name, available_models)\n</code></pre> <p>Returns a checkmark or cross based on model availability.</p> Source code in <code>src/cloudmesh/ai/mesh/servers/base.py</code> <pre><code>def check_model(self, model_name: str, available_models: list) -&gt; str:\n    \"\"\"Returns a checkmark or cross based on model availability.\"\"\"\n    if not available_models:\n        return \"\u2717\"\n\n    # Check if the configured model is among the available models\n    if any(model_name == m for m in available_models):\n        return \"\u2713\"\n\n    # If different, show the first available model\n    return f\"\u2717 {available_models[0]}\"\n</code></pre> probe <code>abstractmethod</code> <pre><code>probe()\n</code></pre> <p>Probes the server for version and model availability. Returns a dictionary with 'version', 'models', and 'status'.</p> Source code in <code>src/cloudmesh/ai/mesh/servers/base.py</code> <pre><code>@abstractmethod\ndef probe(self) -&gt; dict:\n    \"\"\"\n    Probes the server for version and model availability.\n    Returns a dictionary with 'version', 'models', and 'status'.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api.html#cloudmesh.ai.mesh.servers.base-functions","title":"Functions:","text":""},{"location":"api.html#cloudmesh.ai.mesh.servers.ollama","title":"ollama","text":""},{"location":"api.html#cloudmesh.ai.mesh.servers.ollama-classes","title":"Classes","text":"OllamaServer <p>               Bases: <code>BaseServer</code></p> <p>Ollama server probe implementation.</p> Source code in <code>src/cloudmesh/ai/mesh/servers/ollama.py</code> <pre><code>class OllamaServer(BaseServer):\n    \"\"\"Ollama server probe implementation.\"\"\"\n\n    def probe(self) -&gt; dict:\n        try:\n            # Get models\n            models_data = self._request(\"/api/tags\").get(\"models\", [])\n            models = [m.get(\"name\") for m in models_data]\n\n            # Get version - Ollama's /api/version returns a JSON object\n            version = \"Unknown\"\n            try:\n                v_data = self._request(\"/api/version\", timeout=1)\n                version = v_data.get(\"version\", \"Unknown\")\n            except:\n                pass\n\n            return {\n                \"status\": \"up\",\n                \"version\": version,\n                \"models\": models,\n                \"error\": None\n            }\n        except Exception as e:\n            # Use console.error instead of logger to avoid the context_id logging error\n            console.error(f\"Failed to get models from Ollama on {self.host}: {e}\")\n            return {\n                \"status\": \"down\",\n                \"version\": \"N/A\",\n                \"models\": [],\n                \"error\": str(e)\n            }\n</code></pre>"},{"location":"api.html#cloudmesh.ai.mesh.servers.ollama-functions","title":"Functions:","text":""},{"location":"api.html#cloudmesh.ai.mesh.servers.vllm","title":"vllm","text":""},{"location":"api.html#cloudmesh.ai.mesh.servers.vllm-classes","title":"Classes","text":"VllmServer <p>               Bases: <code>BaseServer</code></p> <p>vLLM server probe implementation.</p> Source code in <code>src/cloudmesh/ai/mesh/servers/vllm.py</code> <pre><code>class VllmServer(BaseServer):\n    \"\"\"vLLM server probe implementation.\"\"\"\n\n    def get_models(self) -&gt; list:\n        \"\"\"Queries the vLLM server for available models.\"\"\"\n        try:\n            # vLLM typically follows OpenAI API\n            models_data = self._request(\"/v1/models\").get(\"data\", [])\n            return [m.get(\"id\") for m in models_data]\n        except Exception as e:\n            logger.debug(f\"Failed to query vLLM models on {self.host}: {e}\")\n            return []\n\n    def probe(self) -&gt; dict:\n        try:\n            models = self.get_models()\n\n            # vLLM doesn't have a standard /version endpoint like Ollama\n            # Often the model name is the primary identifier\n            version = models[0] if models else \"Unknown\"\n\n            return {\n                \"status\": \"up\",\n                \"version\": version,\n                \"models\": models,\n                \"error\": None\n            }\n        except Exception as e:\n            # Use console.error instead of logger to avoid the context_id logging error\n            console.error(f\"Failed to get models from vLLM on {self.host}: {e}\")\n            return {\n                \"status\": \"down\",\n                \"version\": \"N/A\",\n                \"models\": [],\n                \"error\": str(e)\n            }\n</code></pre> Methods: get_models <pre><code>get_models()\n</code></pre> <p>Queries the vLLM server for available models.</p> Source code in <code>src/cloudmesh/ai/mesh/servers/vllm.py</code> <pre><code>def get_models(self) -&gt; list:\n    \"\"\"Queries the vLLM server for available models.\"\"\"\n    try:\n        # vLLM typically follows OpenAI API\n        models_data = self._request(\"/v1/models\").get(\"data\", [])\n        return [m.get(\"id\") for m in models_data]\n    except Exception as e:\n        logger.debug(f\"Failed to query vLLM models on {self.host}: {e}\")\n        return []\n</code></pre>"},{"location":"api.html#cloudmesh.ai.mesh.servers.vllm-functions","title":"Functions:","text":""},{"location":"api.html#cloudmesh.ai.mesh.tunnel_manager","title":"tunnel_manager","text":""},{"location":"api.html#cloudmesh.ai.mesh.tunnel_manager-classes","title":"Classes","text":""},{"location":"api.html#cloudmesh.ai.mesh.tunnel_manager.TunnelManager","title":"TunnelManager","text":"<p>Manages SSH tunnels for AI Mesh servers.</p> Source code in <code>src/cloudmesh/ai/mesh/tunnel_manager.py</code> <pre><code>class TunnelManager:\n    \"\"\"Manages SSH tunnels for AI Mesh servers.\"\"\"\n\n    def __init__(self, config_manager: MeshConfigManager = None):\n        self.config_manager = config_manager or MeshConfigManager()\n        self.tunnels = {}\n\n    def _get_server_config(self, hostname: str):\n        \"\"\"Helper to get server config and validate SSH enablement.\"\"\"\n        servers = self.config_manager.get_servers_config()\n        if not servers or hostname not in servers:\n            raise ValueError(f\"Hostname {hostname} not found in configuration.\")\n\n        cfg = servers[hostname]\n        if not cfg.get(\"ssh\", False):\n            raise ValueError(f\"SSH is not enabled for host {hostname} in configuration.\")\n\n        return cfg\n\n    def start(self, hostname: str) -&gt; bool:\n        \"\"\"Starts a tunnel for the specified hostname.\"\"\"\n        cfg = self._get_server_config(hostname)\n\n        ports = cfg.get(\"port\", {})\n        local_port = ports.get(\"local\")\n        remote_port = ports.get(\"remote\")\n\n        if not local_port or not remote_port:\n            raise ValueError(f\"Missing local or remote port configuration for {hostname}.\")\n\n        if hostname in self.tunnels and self.tunnels[hostname].is_active():\n            logger.warn(f\"Tunnel for {hostname} is already running.\")\n            return True\n\n        # remote_host is typically localhost when forwarding to a service on the ssh_host\n        tunnel = Tunnel(\n            local_port=int(local_port),\n            remote_host=\"localhost\",\n            remote_port=int(remote_port),\n            ssh_host=hostname\n        )\n\n        if tunnel.start():\n            self.tunnels[hostname] = tunnel\n            return True\n\n        logger.error(f\"Failed to start tunnel for {hostname}\")\n        return False\n\n    def stop(self, hostname: str) -&gt; bool:\n        \"\"\"Stops the tunnel for the specified hostname.\"\"\"\n        if hostname not in self.tunnels:\n            logger.warn(f\"No active tunnel found for {hostname} to stop.\")\n            return False\n\n        tunnel = self.tunnels[hostname]\n        if tunnel.stop():\n            del self.tunnels[hostname]\n            return True\n\n        return False\n\n    def status(self, hostname: str) -&gt; bool:\n        \"\"\"Returns the status of the tunnel for the specified hostname.\"\"\"\n        # Ensure the host is configured for SSH even for status checks\n        self._get_server_config(hostname)\n\n        if hostname in self.tunnels:\n            return self.tunnels[hostname].is_active()\n\n        return False\n\n    def list_active(self) -&gt; dict:\n        \"\"\"Returns a dictionary of all active tunnels.\"\"\"\n        active_tunnels = {}\n        for host, tunnel in self.tunnels.items():\n            if tunnel.is_active():\n                active_tunnels[host] = {\n                    \"local_port\": tunnel.local_port,\n                    \"remote_port\": tunnel.remote_port,\n                    \"remote_host\": tunnel.remote_host,\n                    \"ssh_host\": tunnel.ssh_host\n                }\n        return active_tunnels\n</code></pre>"},{"location":"api.html#cloudmesh.ai.mesh.tunnel_manager.TunnelManager-functions","title":"Methods:","text":"list_active <pre><code>list_active()\n</code></pre> <p>Returns a dictionary of all active tunnels.</p> Source code in <code>src/cloudmesh/ai/mesh/tunnel_manager.py</code> <pre><code>def list_active(self) -&gt; dict:\n    \"\"\"Returns a dictionary of all active tunnels.\"\"\"\n    active_tunnels = {}\n    for host, tunnel in self.tunnels.items():\n        if tunnel.is_active():\n            active_tunnels[host] = {\n                \"local_port\": tunnel.local_port,\n                \"remote_port\": tunnel.remote_port,\n                \"remote_host\": tunnel.remote_host,\n                \"ssh_host\": tunnel.ssh_host\n            }\n    return active_tunnels\n</code></pre> start <pre><code>start(hostname)\n</code></pre> <p>Starts a tunnel for the specified hostname.</p> Source code in <code>src/cloudmesh/ai/mesh/tunnel_manager.py</code> <pre><code>def start(self, hostname: str) -&gt; bool:\n    \"\"\"Starts a tunnel for the specified hostname.\"\"\"\n    cfg = self._get_server_config(hostname)\n\n    ports = cfg.get(\"port\", {})\n    local_port = ports.get(\"local\")\n    remote_port = ports.get(\"remote\")\n\n    if not local_port or not remote_port:\n        raise ValueError(f\"Missing local or remote port configuration for {hostname}.\")\n\n    if hostname in self.tunnels and self.tunnels[hostname].is_active():\n        logger.warn(f\"Tunnel for {hostname} is already running.\")\n        return True\n\n    # remote_host is typically localhost when forwarding to a service on the ssh_host\n    tunnel = Tunnel(\n        local_port=int(local_port),\n        remote_host=\"localhost\",\n        remote_port=int(remote_port),\n        ssh_host=hostname\n    )\n\n    if tunnel.start():\n        self.tunnels[hostname] = tunnel\n        return True\n\n    logger.error(f\"Failed to start tunnel for {hostname}\")\n    return False\n</code></pre> status <pre><code>status(hostname)\n</code></pre> <p>Returns the status of the tunnel for the specified hostname.</p> Source code in <code>src/cloudmesh/ai/mesh/tunnel_manager.py</code> <pre><code>def status(self, hostname: str) -&gt; bool:\n    \"\"\"Returns the status of the tunnel for the specified hostname.\"\"\"\n    # Ensure the host is configured for SSH even for status checks\n    self._get_server_config(hostname)\n\n    if hostname in self.tunnels:\n        return self.tunnels[hostname].is_active()\n\n    return False\n</code></pre> stop <pre><code>stop(hostname)\n</code></pre> <p>Stops the tunnel for the specified hostname.</p> Source code in <code>src/cloudmesh/ai/mesh/tunnel_manager.py</code> <pre><code>def stop(self, hostname: str) -&gt; bool:\n    \"\"\"Stops the tunnel for the specified hostname.\"\"\"\n    if hostname not in self.tunnels:\n        logger.warn(f\"No active tunnel found for {hostname} to stop.\")\n        return False\n\n    tunnel = self.tunnels[hostname]\n    if tunnel.stop():\n        del self.tunnels[hostname]\n        return True\n\n    return False\n</code></pre>"},{"location":"api.html#cloudmesh.ai.mesh.tunnel_manager-functions","title":"Functions:","text":""},{"location":"note-ollama.html","title":"note-ollama","text":""},{"location":"note-ollama.html#using-ollama-for-local-llm-development","title":"Using Ollama for Local LLM Development","text":""},{"location":"note-ollama.html#overview","title":"Overview","text":"<p>Ollama is a tool that can be used for embeddings and RAG (Retrieval-Augmented Generation) tasks. It offers an alternative to using llama.cpp server mode, providing additional functionality and flexibility.</p>"},{"location":"note-ollama.html#installation-and-setup","title":"Installation and Setup","text":""},{"location":"note-ollama.html#1-install-ollama","title":"1. Install Ollama","text":"<p>To install Ollama, you can follow these steps:</p> <ol> <li> <p>Update System: <pre><code>sudo apt update\nsudo apt upgrade -y\n</code></pre></p> </li> <li> <p>Install Ollama CLI: <pre><code>curl -sSf https://get.ollama.com | sh\n</code></pre></p> </li> <li> <p>Verify Installation: <pre><code>ollama --version\n</code></pre></p> </li> </ol>"},{"location":"note-ollama.html#2-configure-ollama-for-local-use","title":"2. Configure Ollama for Local Use","text":"<ol> <li> <p>Create a Configuration File: <pre><code>mkdir -p ~/ollama/config\nnano ~/ollama/config/config.yaml\n</code></pre>     Add the following configuration:     <pre><code>models:\n  - name: qwen2.5-coder\n    path: ~/models/qwen2.5-coder-32b-q4_k_m.gguf\n</code></pre></p> </li> <li> <p>Run Ollama Server: <pre><code>ollama serve --config ~/ollama/config/config.yaml\n</code></pre></p> </li> </ol>"},{"location":"note-ollama.html#api-endpoints","title":"API Endpoints","text":""},{"location":"note-ollama.html#1-chat-completions","title":"1. Chat Completions","text":"<pre><code>curl http://127.0.0.1:8080/v1/chat/completions \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"model\": \"qwen\",\n    \"messages\": [\n      {\n        \"role\": \"user\",\n        \"content\": \"Write a Rust HTTP server\"\n      }\n    ],\n    \"temperature\": 0.2\n  }'\n</code></pre>"},{"location":"note-ollama.html#2-completions","title":"2. Completions","text":"<pre><code>curl http://127.0.0.1:8080/v1/completions \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"model\": \"qwen\",\n    \"prompt\": \"Write a Rust HTTP server\",\n    \"temperature\": 0.2\n  }'\n</code></pre>"},{"location":"note-ollama.html#3-embeddings","title":"3. Embeddings","text":"<p>```bash curl http://127.0.0.1:8080/v1/embeddings \\   -H \"Content-Type: application/json\" \\   -d '{     \"input\": [\"Write a Rust HTTP server\"]   }'</p>"},{"location":"notes.html","title":"notes","text":""},{"location":"notes.html#i-have-a-nvidia-spark-and-a-desktop-with-128gb-and-an-rtx-3090","title":"I have a NVIDIA Spark and a desktop with 128GB and an RTX 3090","text":"<p>I want to use this setup to run local LLMs for code development. Below is a detailed installation manual for setting up the system without using any paid models, ensuring everything runs locally in user mode.</p>"},{"location":"notes.html#setup-overview","title":"Setup Overview","text":"<p>With a desktop that has 128GB RAM + RTX 3090 (24GB VRAM) plus an NVIDIA Spark, you can build a very capable fully-local coding assistant stack without paying for APIs. </p>"},{"location":"notes.html#core-components","title":"Core Components:","text":"<ul> <li>Inference engine: llama.cpp or vLLM</li> <li>Model manager/UI: Open WebUI</li> <li>IDE integration: Continue.dev</li> <li>Embeddings/RAG: Ollama or llama.cpp server mode</li> <li>Coding models:</li> <li>[Qwen]{.underline} Qwen2.5-Coder</li> <li>[DeepSeek]{.underline} DeepSeek-Coder-V2</li> <li>[Meta]{.underline} Llama 3.1 Instruct</li> <li>[Mistral AI]{.underline} Codestral</li> </ul> <p>You specifically said: - No paid models - Everything local - No daemon/system service - Usermode only</p> <p>So this guide avoids Docker services, systemd daemons, cloud APIs, and paid inference.</p>"},{"location":"notes.html#recommended-architecture","title":"Recommended Architecture","text":"<p>Your hardware is ideal for this split:</p> Machine Role RTX 3090 desktop Main inference server NVIDIA Spark Secondary node / experimentation / embeddings / agents <p>Main development workflow: <pre><code>VSCode / Neovim\n        \u2193\nContinue.dev extension\n        \u2193\nOpenAI-compatible local API\n        \u2193\nllama.cpp server OR vLLM\n        \u2193\nLocal GGUF / AWQ models\n</code></pre></p>"},{"location":"notes.html#best-models-for-your-hardware","title":"Best Models For Your Hardware","text":""},{"location":"notes.html#best-overall-coding-model","title":"Best Overall Coding Model","text":""},{"location":"notes.html#qwen25-coder-32b-instruct-q4_k_m","title":"Qwen2.5-Coder 32B Instruct (Q4_K_M)","text":"<p>Runs extremely well on a 3090 with partial GPU offload.</p> <p>Excellent at: - Python - Rust - C++ - Typescript - Refactoring - Debugging - Agents/tools</p> <p>Recommended GGUF: - Q4_K_M - Q5_K_M if speed acceptable</p> <p>Model source: Qwen2.5-Coder on HuggingFace</p>"},{"location":"notes.html#faster-secondary-model","title":"Faster Secondary Model","text":""},{"location":"notes.html#deepseek-coder-v2-lite","title":"DeepSeek-Coder-V2 Lite","text":"<p>Great autocomplete speed.</p> <p>DeepSeek Coder models</p>"},{"location":"notes.html#massive-context-option","title":"Massive Context Option","text":""},{"location":"notes.html#llama-31-70b-q4","title":"Llama 3.1 70B Q4","text":"<p>Possible because you have 128GB RAM. Not super fast, but very usable.</p>"},{"location":"notes.html#os-recommendations","title":"OS Recommendations","text":"<p>Best: - Ubuntu 24.04 LTS</p> <p>Second best: - Arch Linux</p> <p>Avoid Windows for primary inference if possible. You can use WSL2, but native Linux is much better for CUDA stability and performance.</p>"},{"location":"notes.html#installation-guide","title":"INSTALLATION GUIDE","text":""},{"location":"notes.html#part-1-nvidia-drivers-cuda","title":"PART 1 \u2014 NVIDIA Drivers + CUDA","text":""},{"location":"notes.html#1-update-system","title":"1. Update System","text":"<pre><code>sudo apt update\nsudo apt upgrade -y\n</code></pre>"},{"location":"notes.html#2-install-nvidia-drivers","title":"2. Install NVIDIA Drivers","text":"<p>Check GPU: <pre><code>lspci | grep -i nvidia\n</code></pre> Install recommended drivers: <pre><code>sudo ubuntu-drivers autoinstall\n</code></pre> Reboot.</p> <p>Verify: <pre><code>nvidia-smi\n</code></pre> You should see: - RTX 3090 - VRAM - Driver version</p>"},{"location":"notes.html#part-2-install-cuda-toolkit","title":"PART 2 \u2014 Install CUDA Toolkit","text":""},{"location":"notes.html#install-cuda","title":"Install CUDA","text":"<p>CUDA Toolkit</p> <p>For Ubuntu: <pre><code>sudo apt install nvidia-cuda-toolkit -y\n</code></pre> Verify: <pre><code>nvcc --version\n</code></pre></p>"},{"location":"notes.html#part-3-install-llamacpp","title":"PART 3 \u2014 Install llama.cpp","text":"<p>This is the most important component.</p>"},{"location":"notes.html#clone","title":"Clone","text":"<pre><code>git clone https://github.com/ggml-org/llama.cpp\ncd llama.cpp\n</code></pre>"},{"location":"notes.html#build-with-cuda","title":"Build With CUDA","text":"<p><pre><code>cmake -B build \\\n-DGGML_CUDA=ON\ncmake --build build -j\n</code></pre> Verify: <pre><code>./build/bin/llama-cli --help\n</code></pre></p>"},{"location":"notes.html#part-4-download-models","title":"PART 4 \u2014 Download Models","text":"<p>Create model directory: <pre><code>mkdir -p ~/models\ncd ~/models\n</code></pre> Install huggingface downloader: <pre><code>pip install -U huggingface_hub\n</code></pre></p>"},{"location":"notes.html#download-qwen25-coder","title":"Download Qwen2.5-Coder","text":"<p>Example: <pre><code>huggingface-cli download \\\nQwen/Qwen2.5-Coder-32B-Instruct-GGUF \\\n--include \"*Q4_K_M.gguf*\" \\\n--local-dir .\n</code></pre></p>"},{"location":"notes.html#part-5-run-inference-server-usermode-only","title":"PART 5 \u2014 Run Inference Server (USERMODE ONLY)","text":"<p>NO daemon. NO system service. Manual terminal launch only.</p>"},{"location":"notes.html#launch-llamacpp-openai-compatible-server","title":"Launch llama.cpp OpenAI-Compatible Server","text":"<p><pre><code>cd ~/llama.cpp\n\n./build/bin/llama-server \\\n  -m ~/models/qwen2.5-coder-32b-q4_k_m.gguf \\\n  --host 127.0.0.1 \\\n  --port 8080 \\\n  -ngl 999 \\\n  -c 32768\n</code></pre> Explanation:</p> Option Meaning -m model --host localhost only --port API port -ngl 999 offload all layers possible to GPU -c 32768 32k context"},{"location":"notes.html#important-api-routes","title":"IMPORTANT API ROUTES","text":"<p>llama.cpp exposes OpenAI-compatible routes.</p> <p>These are the routes you integrate into programs.</p>"},{"location":"notes.html#base-url","title":"Base URL","text":"<pre><code>http://127.0.0.1:8080\n</code></pre>"},{"location":"notes.html#route-chat-completions","title":"ROUTE: Chat Completions","text":"<p><pre><code>POST /v1/chat/completions\n</code></pre> Equivalent to OpenAI API. Example: <pre><code>curl http://127.0.0.1:8080/v1/chat/completions \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"model\": \"qwen\",\n    \"messages\": [\n      {\n        \"role\": \"user\",\n        \"content\": \"Write a Rust HTTP server\"\n      }\n    ],\n    \"temperature\": 0.2\n  }'\n</code></pre></p>"},{"location":"notes.html#route-completions","title":"ROUTE: Completions","text":"<p><pre><code>POST /v1/completions\n</code></pre> Legacy completion endpoint.</p>"},{"location":"notes.html#route-embeddings","title":"ROUTE: Embeddings","text":"<p><pre><code>POST /v1/embeddings\n</code></pre> Useful for: - Semantic search - RAG - Codebase indexing</p>"},{"location":"notes.html#route-health","title":"ROUTE: Health","text":"<pre><code>GET /health\n</code></pre>"},{"location":"notes.html#route-tokenize","title":"ROUTE: Tokenize","text":"<pre><code>POST /tokenize\n</code></pre>"},{"location":"notes.html#part-6-vscode-integration","title":"PART 6 \u2014 VSCode Integration","text":"<p>Install: Continue.dev</p>"},{"location":"notes.html#continue-config","title":"Continue Config","text":"<p>Config file: <pre><code>~/.continue/config.json\n</code></pre> Example: <pre><code>{\n  \"models\": [\n    {\n      \"title\": \"Qwen Local\",\n      \"provider\": \"openai\",\n      \"model\": \"qwen\",\n      \"apiBase\": \"http://127.0.0.1:8080/v1\",\n      \"apiKey\": \"local\"\n    }\n  ]\n}\n</code></pre> Now: - Autocomplete - Inline edits - Codebase chat - Refactors - All local.</p>"},{"location":"notes.html#part-7-open-webui-optional","title":"PART 7 \u2014 Open WebUI (Optional)","text":"<p>This gives you ChatGPT-style local UI. Open WebUI GitHub Since you do NOT want daemons: Run manually: <pre><code>python -m venv venv\nsource venv/bin/activate\n\npip install open-webui\n</code></pre> Launch: <pre><code>open-webui serve\n</code></pre> Then connect: - API URL: <code>http://127.0.0.1:8080</code></p>"},{"location":"notes.html#part-8-better-performance","title":"PART 8 \u2014 Better Performance","text":""},{"location":"notes.html#recommended-runtime-flags","title":"Recommended Runtime Flags","text":"<p>For 3090: <pre><code>--flash-attn\n</code></pre> and <pre><code>--cache-type-k q8_0\n--cache-type-v q8_0\n</code></pre> Example: <pre><code>./build/bin/llama-server \\\n  -m model.gguf \\\n  -ngl 999 \\\n  --flash-attn \\\n  --cache-type-k q8_0 \\\n  --cache-type-v q8_0\n</code></pre></p>"},{"location":"notes.html#part-9-multi-gpu-nvidia-spark-usage","title":"PART 9 \u2014 Multi-GPU / NVIDIA Spark Usage","text":"<p>If Spark has CUDA capability: - Dedicate Spark to embeddings - Run smaller agent models - Run code search - Run speech/TTS locally</p> <p>Useful split:</p> Device Task 3090 main coding LLM Spark embeddings / agent loops"},{"location":"notes.html#part-10-advanced-setup-recommended","title":"PART 10 \u2014 Advanced Setup (Recommended)","text":"<p>Once basics work: Install: - Aider - OpenHands - TabbyML</p> <p>These all support local OpenAI-compatible endpoints.</p>"},{"location":"notes.html#aider-configuration","title":"Aider Configuration","text":"<p><pre><code>pip install aider-chat\n</code></pre> Run: <pre><code>aider \\\n  --model openai/qwen \\\n  --openai-api-base http://127.0.0.1:8080/v1 \\\n  --openai-api-key local\n</code></pre> This gives you: - Repository-aware edits - Autonomous refactoring - Local coding agent</p>"},{"location":"notes.html#recommended-final-stack","title":"Recommended Final Stack","text":""},{"location":"notes.html#minimal-stable-setup","title":"Minimal Stable Setup","text":"<pre><code>llama.cpp\n+\nQwen2.5-Coder 32B\n+\nContinue.dev\n+\nVSCode\n</code></pre>"},{"location":"notes.html#best-power-user-setup","title":"Best Power User Setup","text":"<pre><code>llama.cpp\n+\nOpen WebUI\n+\nAider\n+\nContinue.dev\n+\nQwen2.5-Coder 32B\n+\nDeepSeek Coder Lite\n</code></pre>"},{"location":"notes.html#expected-performance","title":"Expected Performance","text":"<p>On RTX 3090:</p> Model Speed 7B 80\u2013140 tok/s 14B 40\u201370 tok/s 32B Q4 18\u201335 tok/s 70B Q4 5\u201310 tok/s"},{"location":"notes.html#what-not-to-use","title":"What NOT To Use","text":"<p>Avoid: - Ollama for serious tuning/control - Docker-first setups - systemd services - Cloud relay UIs - Electron-heavy wrappers</p> <p>llama.cpp directly is cleaner and faster.</p>"},{"location":"notes.html#recommended-directory-layout","title":"Recommended Directory Layout","text":"<pre><code>~/ai/\n\u251c\u2500\u2500 llama.cpp/\n\u251c\u2500\u2500 models/\n\u251c\u2500\u2500 embeddings/\n\u251c\u2500\u2500 projects/\n\u251c\u2500\u2500 scripts/\n\u2514\u2500\u2500 logs/\n</code></pre>"},{"location":"notes.html#useful-launch-script","title":"Useful Launch Script","text":"<p><pre><code>#!/bin/bash\n\nMODEL=~/models/qwen2.5-coder-32b-q4_k_m.gguf\n\n~/llama.cpp/build/bin/llama-server \\\n  -m $MODEL \\\n  -ngl 999 \\\n  --host 127.0.0.1 \\\n  --port 8080 \\\n  --flash-attn \\\n  -c 32768\n</code></pre> Run manually whenever needed: <pre><code>bash start-llm.sh\n</code></pre> Still usermode only. No daemon.</p>"},{"location":"notes.html#part-10-spark-node","title":"PART 10 \u2014 SPARK NODE","text":""},{"location":"notes.html#on-spark","title":"On spark","text":"<p>Create Docker network: <pre><code>docker network create ai-stack\n</code></pre></p>"},{"location":"notes.html#embedding-server","title":"Embedding Server","text":"<p>Run: <pre><code>docker run -d \\\n  --name embeddings \\\n  --gpus all \\\n  --network ai-stack \\\n  -p 8090:80 \\\n  ghcr.io/huggingface/text-embeddings-inference:cuda-latest \\\n  --model-id BAAI/bge-large-en-v1.5\n</code></pre></p>"},{"location":"notes.html#qdrant-vector-db","title":"QDRANT VECTOR DB","text":"<pre><code>docker run -d \\\n  --name qdrant \\\n  --network ai-stack \\\n  -p 6333:6333 \\\n  -v ~/ai-stack/data/qdrant:/qdrant/storage \\\n  qdrant/qdrant\n</code></pre>"},{"location":"notes.html#spark-apis","title":"Spark APIs","text":"<p>Embeddings: <pre><code>http://spark:8090/embed\n</code></pre> Qdrant: <pre><code>http://spark:6333\n</code></pre></p>"},{"location":"notes.html#part-11-continuedev","title":"PART 11 \u2014 CONTINUE.DEV","text":"<p>Install: - VSCode - Continue extension</p> <p>Continue.dev Docs</p>"},{"location":"notes.html#continue-config_1","title":"Continue Config","text":"<pre><code>{\n  \"models\": [\n    {\n      \"title\": \"Local Qwen\",\n      \"provider\": \"openai\",\n      \"model\": \"qwen\",\n      \"apiBase\": \"http://white:4000/v1\",\n      \"apiKey\": \"local\"\n    }\n  ]\n}\n</code></pre>"},{"location":"notes.html#part-12-aider","title":"PART 12 \u2014 AIDER","text":"<p>Install on laptop: <pre><code>pip install aider-chat\n</code></pre> Run: <pre><code>aider \\\n  --model openai/qwen \\\n  --openai-api-base http://white:4000/v1 \\\n  --openai-api-key local\n</code></pre></p>"},{"location":"notes.html#part-13-useful-commands","title":"PART 13 \u2014 USEFUL COMMANDS","text":""},{"location":"notes.html#ssh","title":"SSH","text":"<pre><code>ssh white\nssh spark\n</code></pre>"},{"location":"notes.html#check-containers","title":"Check containers","text":"<pre><code>docker ps\n</code></pre>"},{"location":"notes.html#logs","title":"Logs","text":"<pre><code>docker logs llama-qwen\n</code></pre>"},{"location":"notes.html#restart-stack","title":"Restart stack","text":"<pre><code>docker compose restart\n</code></pre>"},{"location":"notes.html#stop-stack","title":"Stop stack","text":"<pre><code>docker compose down\n</code></pre>"},{"location":"notes.html#final-endpoints","title":"FINAL ENDPOINTS","text":"Service URL LiteLLM <code>http://white:4000/v1</code> llama.cpp <code>http://white:8080</code> llama-swap <code>http://white:8082</code> Open WebUI <code>http://white:3000</code> embeddings <code>http://spark:8090/embed</code> qdrant <code>http://spark:6333</code>"},{"location":"notes.html#recommended-next-steps","title":"RECOMMENDED NEXT STEPS","text":"<p>After stable deployment: - TabbyML - OpenHands - Langfuse - SearXNG</p> <p>But get the core inference stable FIRST.</p> <p>I created a downloadable control-plane package for your laptop that includes: - Deployment scripts - Cluster control scripts - Docker Compose files - LiteLLM config - Orchestration wrapper (<code>ai</code>) - Separate configs for <code>white</code> and <code>spark</code></p> <p>Download it here: Download the llm-local control package</p> <p>After extracting on your laptop: <pre><code>unzip llm-local-control.zip\ncd llm-local-control\n</code></pre> Recommended destination: <pre><code>mv llm-local-control ~/llm-local\n</code></pre> Then deploy: <pre><code>cd ~/llm-local\n./control/deploy.sh\n</code></pre> Then start the cluster: <pre><code>./control/start-all.sh\n</code></pre> Or use the unified command: <pre><code>./control/ai start\n</code></pre> Useful commands: <pre><code>./control/ai status\n./control/ai logs white llama-qwen\n./control/ai restart white llama-qwen\n./control/ai stop\n</code></pre> Also add that I can call the <code>ai</code> command directly without specifying the path.</p> <p>After moving the package to: <pre><code>~/llm-local\n</code></pre> Add the control directory to your shell <code>PATH</code>.</p>"},{"location":"prompt.html","title":"prompt","text":""},{"location":"prompt.html#system-prompt-local-ai-cluster-architect","title":"SYSTEM PROMPT: Local AI Cluster Architect","text":""},{"location":"prompt.html#persona","title":"\ud83c\udfad PERSONA","text":"<p>You are the Senior AI Systems Architect and Orchestrator for a fully local, air-gapped AI development cluster. Your primary goal is to provide expert software engineering assistance while managing the underlying distributed compute infrastructure.</p>"},{"location":"prompt.html#cluster-architecture","title":"\ud83c\udfd7\ufe0f CLUSTER ARCHITECTURE","text":"<p>The system consists of three distinct nodes. You must always be aware of where a process is running.</p>"},{"location":"prompt.html#gemma-ultra-ultra-high-performance-tier","title":"\ud83c\udf0c GEMMA-ULTRA (Ultra-High Performance Tier)","text":"<ul> <li>Hardware: 4x A100 80GB GPUs</li> <li>Core Services:</li> <li>vLLM: Enterprise-grade inference server (Port 18123).</li> <li>Role: Tier 0 resource. Use for maximum-complexity reasoning, massive context windows, and critical architectural decisions.</li> </ul>"},{"location":"prompt.html#white-primary-compute-node","title":"\ud83d\udda5\ufe0f WHITE (Primary Compute Node)","text":"<ul> <li>Hardware: RTX 3090 + 128GB RAM</li> <li>Core Services:</li> <li>vLLM: High-performance GPU inference server.</li> <li>LiteLLM: Central API Gateway (Port 4000). All client requests enter here.</li> <li>Role: Primary execution target for standard high-reasoning and coding tasks.</li> </ul>"},{"location":"prompt.html#spark-secondaryfallback-node","title":"\u26a1 SPARK (Secondary/Fallback Node)","text":"<ul> <li>Hardware: NVIDIA GPU Node</li> <li>Core Services:</li> <li>vLLM: GPU inference server (Port 8080).</li> <li>Role: High-availability fallback. If WHITE is unavailable, LiteLLM automatically routes traffic here.</li> </ul>"},{"location":"prompt.html#laptop-control-plane","title":"\ud83d\udcbb LAPTOP (Control Plane)","text":"<ul> <li>Hardware: Client Machine</li> <li>Core Services:</li> <li>VSCode: Development environment.</li> <li>Cluster Agent: Python-based orchestration tool.</li> <li>Role: Command and control. All deployment and management commands originate here.</li> </ul>"},{"location":"prompt.html#operational-logic-routing","title":"\ud83d\udea6 OPERATIONAL LOGIC &amp; ROUTING","text":"<ul> <li>Entrypoint: <code>http://localhost:4000/v1/chat/completions</code> (LiteLLM on WHITE).</li> <li>Routing: LiteLLM handles the abstraction. You do not need to change URLs to switch nodes; LiteLLM manages the fallback chain: GEMMA-ULTRA $\\rightarrow$ WHITE $\\rightarrow$ SPARK.</li> <li>Model Selection: Use specific model names to signal intent:</li> <li><code>gemma-ultra</code>: For maximum power/complexity.</li> <li><code>gpu-primary</code>: For standard high-performance tasks.</li> <li><code>cpu-fallback</code>: For basic availability.</li> </ul>"},{"location":"prompt.html#technology-deployment-rules","title":"\ud83d\udee0\ufe0f TECHNOLOGY &amp; DEPLOYMENT RULES","text":"<ul> <li>Docker First: All services MUST run in Docker containers. </li> <li>No Host Pollution: Avoid suggesting <code>apt-get</code> or <code>pip install</code> on the host. Use <code>docker exec</code> or update the <code>docker-compose.yml</code> files.</li> <li>Explicit Execution: Every command you generate MUST be prefixed with the target node.</li> </ul>"},{"location":"prompt.html#safety-constraints","title":"\u26a0\ufe0f SAFETY &amp; CONSTRAINTS","text":"<ul> <li>Air-Gapped: The system is 100% offline. No external API calls, no internet downloads, no telemetry.</li> <li>Transparency: No silent execution. Every system change must be presented as a clear, manual step for the user.</li> <li>State Awareness: Do not assume a service is running. If a command fails, suggest checking logs: <code>docker compose logs -f [service]</code>.</li> </ul>"},{"location":"prompt.html#output-formatting-rule","title":"\ud83d\udccb OUTPUT FORMATTING RULE","text":"<p>When providing technical instructions or commands, you MUST use the following structure:</p>"},{"location":"prompt.html#white-rtx-3090-node","title":"\ud83d\udda5\ufe0f WHITE (RTX 3090 node)","text":"<pre><code># Commands for WHITE here\n</code></pre>"},{"location":"prompt.html#spark-nvidia-node","title":"\u26a1 SPARK (NVIDIA node)","text":"<pre><code># Commands for SPARK here\n</code></pre>"},{"location":"prompt.html#laptop-control-plane_1","title":"\ud83d\udcbb LAPTOP (control plane)","text":""},{"location":"prompt.html#commands-for-laptop-here","title":"<pre><code># Commands for LAPTOP here\n</code></pre>","text":"<p>Failure to label the target machine is a critical error.</p>"}]}